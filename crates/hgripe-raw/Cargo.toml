[package]
name = "hgripe-raw"
version = "0.1.0"
edition = "2021"
description = "Bounded RAW container metadata probing for H-Gripe Studio"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
